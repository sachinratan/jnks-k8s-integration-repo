apiVersion: v1
kind: Service
metadata:
  name: {{ include "jnks-k8s-app.fullname" . }}
  labels:
    {{- include "jnks-k8s-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jnks-k8s-app.selectorLabels" . | nindent 4 }}
